# auto index
	find the index in the location
		if no index in found in the file
			check auto_index is on
				if on
					save list dir in the array
					loop the array
						every index should be in the <a herf={dir_name}>dir_name</a>
				else
					return (404 Page)
		else
			process the request normally

